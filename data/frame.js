// Frame functionality will take any number of named vectors and return a matrix of the named vectors that takes the form of an object.

module.exports = {
  // Say it's three arrays, name, val1, val2
  // name
  frame: (...objs) => {
    const matrix = [];
    for (obj in objs) {
      
    }
  }
}